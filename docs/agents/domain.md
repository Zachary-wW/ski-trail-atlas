# Domain Docs

Before exploring, read the root `CONTEXT.md` and relevant ADRs under `docs/adr/` when they exist; proceed silently if absent.

## Layout

This is a single-context repository:

```text
/
├── CONTEXT.md
├── docs/adr/
└── src/
```

Use vocabulary defined in `CONTEXT.md`. Explicitly flag proposals that conflict with an existing ADR.
